import { config } from 'dotenv'
config()

const {
  NODE_ENV,

  APP_PORT,
  APP_LOGO,
  APP_NAME,

  MONGODB_URL,
  DB_COLLECTION_NAME,

  ACCESS_TOKEN_SECRET,
  SESSION_SECRET_KEY,

  GOOGLE_CLIENT_SECRET,
  GOOGLE_CLIENT_ID,

  BACKEND_URL,

  FRONTEND_URL,

  MAIL_PORT,
  MAIL_HOST,
  MAIL_PASSWORD,
  MAIL_FROM,
  MAIL_USERNAME,
  MAIL_SECURE,

  CLOUD_NAME,
  CLOUD_API_KEY,
  CLOUD_API_SECRET,
} = process.env

const ENV = {
  NODE_ENV,

  APP_PORT,
  APP_NAME,
  APP_LOGO,

  FRONTEND_URL,
  BACKEND_URL,

  MONGODB_URL,
  DB_COLLECTION_NAME,

  ACCESS_TOKEN_SECRET,
  SESSION_SECRET_KEY,

  GOOGLE_CLIENT_SECRET,
  GOOGLE_CLIENT_ID,

  MAIL_PORT,
  MAIL_HOST,
  MAIL_PASSWORD,
  MAIL_FROM,
  MAIL_USERNAME,
  MAIL_SECURE,

  CLOUD_NAME,
  CLOUD_API_KEY,
  CLOUD_API_SECRET,
}

export default ENV
